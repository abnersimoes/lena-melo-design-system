import { css } from 'lit';

export default css`
  :host {
    width: 100%;
    max-width: 80rem;
    margin-left: auto;
    margin-right: auto;
    padding-left: var(--spacing-inset-sm);
    padding-right: var(--spacing-inset-sm);
    display: flex;
  }
`;
